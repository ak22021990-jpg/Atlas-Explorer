# Archived on 2026-04-27

These files were moved out of the active app because they are not part of the runtime game or test path.

Archived groups:
- `assets/generated/`, `js/canva-game-sprite.js`, `js/generated/`, `tools/`: optional Canva sprite import pipeline
- `docs/`, `graphify-out/`, `screenshots/`, `implementation_plan.md`, `stitch_instructions.md`, `test.md`: planning, generated analysis, and reference material
- `.claude/`, `generate_map.py`, `screenshot_atlas.mjs`: local tooling and one-off utilities not used by the app

Active app paths remain in the project root (`index.html`, `game.html`, `css/`, `js/`, `data/`, `maps/`, `apps-script/`, `tests/`).
